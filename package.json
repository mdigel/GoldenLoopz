{
  "name": "golden-loopz",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "dev": "npx expo run:ios",
    "web": "expo start --web",
    "lint": "expo lint",
    "format": "npx prettier --write .",
    "typecheck": "tsc --noEmit",
    "check:dummy-data": "node -e \"const fs=require('fs');const path='src/config/appConfig.ts';const content=fs.readFileSync(path,'utf8');const m=content.match(/const\\s+LOCAL_DUMMY_DATA\\s*=\\s*(true|false)\\s*;/);if(!m){console.error('Missing LOCAL_DUMMY_DATA in '+path);process.exit(1);}if(m[1]==='true'){console.error('LOCAL_DUMMY_DATA is true. Disable before building.');process.exit(1);}console.log('Dummy data disabled for build.');\"",
    "prebuild-check": "npm run check:dummy-data && tsc --noEmit && npx expo export --platform ios --output-dir .expo-check --clear && rm -rf .expo-check && echo '✅ Pre-build check passed'",
    "full-rebuild": "rm -rf node_modules && npm install --legacy-peer-deps && npx expo run:ios",
    "version:bump": "node -e \"const fs=require('fs'); const app=JSON.parse(fs.readFileSync('app.json')); const v=app.expo.version.split('.'); v[2]=parseInt(v[2])+1; app.expo.version=v.join('.'); fs.writeFileSync('app.json',JSON.stringify(app,null,2)+'\\n'); console.log('Bumped to '+app.expo.version);\"",
    "build:ios": "npm run check:dummy-data && npm run version:bump && npx eas-cli build --platform ios",
    "build:ios:submit": "npm run check:dummy-data && npm run version:bump && npx eas-cli build --platform ios --auto-submit"
  },
  "dependencies": {
    "@react-native-async-storage/async-storage": "2.2.0",
    "@react-native-community/slider": "5.0.1",
    "@react-navigation/bottom-tabs": "^7.9.0",
    "@react-navigation/native": "^7.1.26",
    "@react-navigation/native-stack": "^7.9.0",
    "date-fns": "^4.1.0",
    "expo": "~54.0.31",
    "expo-haptics": "~15.0.8",
    "expo-linear-gradient": "~15.0.8",
    "expo-status-bar": "~3.0.9",
    "lucide-react-native": "^0.544.0",
    "nativewind": "^4.2.1",
    "posthog-react-native": "^4.17.3",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "react-native-css-interop": "0.1.22",
    "react-native-gesture-handler": "~2.28.0",
    "react-native-reanimated": "~4.1.1",
    "react-native-safe-area-context": "~5.6.0",
    "react-native-screens": "~4.16.0",
    "react-native-svg": "15.12.1",
    "react-native-worklets": "^0.7.1",
    "tailwindcss": "^3.4.19",
    "zustand": "^5.0.9"
  },
  "devDependencies": {
    "@types/react": "~19.1.0",
    "babel-preset-expo": "^54.0.9",
    "typescript": "~5.9.2"
  },
  "private": true
}
